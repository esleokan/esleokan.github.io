/**
 * Lazy Loading Module
 * 目前網站未使用懶載入功能，保留模組以備將來使用
 */

// Initialize lazy loading - 簡化版本
export function initLazyLoading() {
  // 目前網站圖片都使用 loading="eager" 或 loading="lazy" 屬性
  // 此模組保留以維持相容性，如需啟用可取消註解下方代碼
  
  /*
  // Look for all images with data-src attribute
  const images = document.querySelectorAll('img[data-src]');
  
  if (images.length > 0) {
    // Setup Intersection Observer...
  }
  */
} 